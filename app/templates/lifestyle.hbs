<div>
  {{videos-table
    videos=model
    category=category}}
  <footer>
    <button class="previous-btn"  {{action 'prev'}}>Previous</button>
    <button class="next-btn"  {{action 'next'}}>Next</button>
  </footer>
</div>
